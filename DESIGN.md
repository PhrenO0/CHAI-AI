# 디자인 시스템 — 청호나이스 2030 브랜드 커뮤니케이션 전략 기획서

세 레퍼런스를 하나의 디자인 언어로 융합했다.

| 레퍼런스 | 가져온 것 |
|---|---|
| Behance "코지 정수기" (첨부 htm) | 전략 케이스스터디 문법: 섹션 마커, 넘버드 블루 필, 더블다이아몬드/체인, 페르소나, Pain→Solution, 밸류트리, 서비스 블루프린트, 마찰 레드닷 |
| Apple (DESIGNapple.md) | 절제: 단일 강조색, 타이트 자간, 넉넉한 여백, 라이트/다크 타일 교차, 그림자 1종, 장식 그라디언트 없음 |
| 블루 PPT 템플릿 (첨부 이미지) | 덱 관례: 표지, PART 디바이더 대형 숫자, 목차, 마무리 |

## 컬러 토큰 (htm에서 실제 추출)

| 토큰 | 값 | 용도 |
|---|---|---|
| `--blue` | `#0057ff` | **단일 강조색.** 링크·강조·CTA·마커. 이 파랑 외 강조색 없음 |
| `--blue-fill` | `#1e9be6` | 넘버드 배지·서클 채움 |
| `--blue-on-dark` | `#2997ff` | 다크 타일 위 강조 |
| `--ink` | `#191919` | 헤드라인·본문 잉크 |
| `--body` `--muted` `--muted-2` | `#3c3c3c` `#707070` `#959595` | 본문·보조·캡션 |
| `--hair` `--hair-soft` | `#e8e8e8` `#f0f0f0` | 헤어라인·트랙 |
| `--canvas` `--parchment` `--dark` | `#fff` `#f5f5f7` `#191919` | 캔버스·파치먼트·다크 타일 |
| `--red` | `#e5484d` | 마찰점 표시(레드닷). 의미색이라 강조색과 구분 |

## 타이포

- 서체: `Pretendard` → 폴백 `-apple-system, "Apple SD Gothic Neo", "Noto Sans KR", system-ui`.
  Pretendard는 CDN으로 향상되며, 없어도 폴백으로 정상 렌더된다(Artifact CSP 안전).
- 헤딩 700~800 / 음수 자간(-0.02 ~ -0.035em)으로 "타이트한" 국문 헤드라인.
- 스케일: `.h-hero`(60) · `.h-title`(40) · `.h-lg`(33) · `.h-md`(25) · `.lead`(19) · `.body`(16) · `.small`(14) · `.src`(8.5, 출처 전용).

## 레이아웃 & 그림자

- 슬라이드: 고정 `1280×720`(16:9). 화면에서는 JS가 뷰포트 폭에 맞춰 `--z`(zoom) 설정. 인쇄 시 `zoom:1` + `@page 1280×720`로 슬라이드당 1페이지.
- 간격: 8px 베이스(`--s1`~`--s9`). 슬라이드 안쪽 여백 72px.
- 라운드: pill(배지·CTA·칩) · 18px(카드) · 12px(작은 카드) · 8px(유틸) · 0(풀블리드 타일).
- **그림자는 시스템 전체에서 이미지 프레임 1종(`--shadow-img`)만.** 카드·버튼·텍스트에는 쓰지 않는다. 위계는 서피스 색 전환(라이트↔다크)으로 만든다.

## 컴포넌트 (슬라이드별 레이아웃 원형)

장마다 구성을 다르게 해서 "3개 카드 반복"의 AI 덱 티를 피한다.

- `marker` 섹션 마커(블루 바+라벨) · `numbadge`/`numpill` 넘버드 블루 필
- `flow` 전환 칩+화살표(02) · `chain` 세로 연결 서사(03)
- `wall` Message Wall(04) · `thumbs` 썸네일 감사 그리드(05) · `tl` 이중 타임라인(06)
- `matrix` 2×2(07 타깃 / 14 결정) · `day` 하루 시나리오+레드닷(08) · `bars` 시나리오 누적 막대(09)
- `ptable`/`hub` 제품 중심 표(11) · `effect`+`stepline` 효과·흐름(12) · `ba` Before/After(13)
- `journey` 가로 여정(16) · 목업 그리드(17) · `zones` 팝업 동선(18) · `funnel` 퍼널(19) · `change` 변화 4열(20)

## Do / Don't

**Do** — 강조는 오직 `--blue`. 헤딩은 판단 문장. 서피스 전환으로 리듬. 이미지 프레임에만 그림자. 근거 있는 숫자만.
**Don't** — 두 번째 강조색 금지. 카드·버튼 그림자 금지. 장식 그라디언트·이모지 아이콘 금지. em대시(—) 금지. 모든 장 같은 3카드 금지. 근거 없는 숫자 금지.

## 이미지

제품·펭귄·경쟁사 광고·페르소나·목업은 CSS 플레이스홀더 프레임 + 라벨(`[에스프레카페 제품]` 등)로 둔다. 브랜드 자산은 날조하지 않는다. 실사진/AI 이미지는 이후 교체한다.
